<?php
declare(strict_types=1);

namespace Willow\Models;

use DateTime;

/**
{% for column in column_list %}
 * @property {{ column.type }} ${{ column.column_name }}
{% endfor %}
 */
#[ApplyModelRule(ModelDefaultRules::class)]
#[ApplyModelRule({{ class_name }}ModelRules::class)]
{% for column in column_list %}
#[ApplyModelColumnAttributes('{{ column.column_name }}', '{{ column.type }}')]
{% endfor %}
class {{ class_name }} extends ModelBase
{
    protected $table = '{{ table }}';
}
