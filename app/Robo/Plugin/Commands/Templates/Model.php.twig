<?php
declare(strict_types=1);

namespace Willow\Models;

use DateTime;

/**
{% for column in column_list %}
 * @property {{ column.type }} ${{ column.name }}
{% endfor %}
 */
#[ApplyModelRule(ModelDefaultRules::class)]
#[ApplyModelRule({{ class_name }}ModelRules::class)]
{% for column in column_list %}
#[ApplyModelColumnAttributes('{{ column.name }}', '{{ column.type }}', {{ column.length }}, [{% for flag in column.flags %} '{{ flag|e }}', {% endfor %}])]
{% endfor %}
class {{ class_name }} extends ModelBase
{
    protected $table = '{{ table }}';
}
