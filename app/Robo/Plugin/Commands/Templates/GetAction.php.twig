<?php
declare(strict_types=1);

namespace Willow\Controllers\{{ class_name }};

use Willow\Controllers\GetActionBase;

abstract class {{ class_name }}GetAction extends GetActionBase
{
    /**
     * {{ class_name }}GetAction constructor.
     * @param {{ class_name }} $model
     */
    public function __construct({{ class_name }} $model) {
        $this->model = $model;
    }
}
