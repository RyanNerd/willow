<?php
declare(strict_types=1);

namespace Willow\Controllers\{{ class_name }};

use Willow\Controllers\GetActionBase;
use Willow\Models\{{ class_name }};

class {{ class_name }}GetAction extends GetActionBase
{
    /**
     * Get the model via Dependency Injection and save it as a property.
     *
     * @param {{ class_name }} $model
     */
    public function __construct({{ class_name }} $model)
    {
        $this->setModel($model);
    }
}
