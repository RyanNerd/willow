<?php
declare(strict_types=1);

namespace Willow\Controllers\{{ class_name }};

use Willow\Controllers\SearchValidatorBase;
use Willow\Models\{{ class_name }};

class {{ class_name }}SearchValidator extends SearchValidatorBase
{
    /**
     * {{ class_name }}SearchValidator constructor.
     * @param {{ class_name }} $model
     */
    public function __construct({{ class_name }} $model)
    {
        $this->setModel($model);
    }
}
