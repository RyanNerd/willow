<?php
declare(strict_types=1);

namespace Willow\Controllers\{{ class_name }};

use Willow\Controllers\DeleteActionBase;
use Willow\Models\{{ class_name }};

class {{ class_name }}DeleteAction extends DeleteActionBase
{
    /**
     * Get the model via Dependency Injection and save it.
     *
     * @param {{ class_name }} $model
     */
    public function __construct({{ class_name }} $model)
    {
        $this->setModel($model);
    }
}
