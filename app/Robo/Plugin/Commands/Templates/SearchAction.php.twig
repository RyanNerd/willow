<?php
declare(strict_types=1);

namespace Willow\Controllers\{{ class_name }};

use Willow\Controllers\SearchActionBase;
use Willow\Models\{{ class_name }};


class {{ class_name }}SearchAction extends SearchActionBase
{
    /**
     * {{ class_name }}SearchAction constructor.
     * @param {{ class_name }} $model
     */
    public function __construct({{ class_name }} $model) {
        $this->model = $model;
    }
}
